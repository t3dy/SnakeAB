{
  "name": "snakeab",
  "version": "0.1.0",
  "description": "Narrative roguelike autobattler — draft snakes, watch emergent storytelling",
  "type": "module",
  "main": "src/main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/**/*.test.js",
    "test:watch": "node --test --watch tests/**/*.test.js"
  },
  "dependencies": {
    "seedrandom": "^3.0.5"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
